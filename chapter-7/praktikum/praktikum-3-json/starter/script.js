const dataMahasiswa = {
    name: "Harry Rosianto",
    major: "Ilmu Komputer",
    skills: ["HTML & CSS", "JavaScript"],
};

// TODO: gunakan JSON.stringify(dataMahasiswa) untuk mengonversi Object menjadi teks JSON,
// simpan ke variabel teksJSON, lalu tampilkan teksJSON dan typeof teksJSON ke Console


// TODO: gunakan JSON.parse(teksJSON) untuk mengonversi kembali menjadi Object,
// simpan ke variabel dataKembali, lalu tampilkan dataKembali, typeof dataKembali,
// dan dataKembali.name ke Console
